module Math where

add :: Float -> Float -> Float
add x y = x + y

sub :: Float -> Float -> Float
sub x y = x - y

mult :: Float -> Float -> Float
mult x y = x * y

divide :: Float -> Float -> Float
divide x y = x / y
